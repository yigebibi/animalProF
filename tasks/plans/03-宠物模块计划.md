# 宠物模块开发计划

---

## 📋 目录

1. [概述](#概述)
2. [功能需求](#功能需求)
3. [技术实现](#技术实现)
4. [开发进度](#开发进度)
5. [接口列表](#接口列表)
6. [测试计划](#测试计划)
7. [验收标准](#验收标准)
8. [风险评估](#风险评估)

---

## 概述

### 模块名称
宠物模块 (Pet Module)

### 开发时间
**计划开始**: 2026-04-24  
**计划完成**: 2026-04-26  
**负责人**: 后端开发团队

### 模块说明
提供宠物信息管理功能，包括添加宠物、编辑信息、删除宠物等操作。

### 依赖关系
- **认证模块**: 需要用户身份验证
- **数据库**: PostgreSQL 存储宠物信息
- **文件模块**: 宠物头像上传功能

---

## 功能需求

### 基本管理

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 添加宠物 | 创建宠物信息（名字、品种、生日、性别等） | P0 |
| 查看宠物列表 | 获取用户的所有宠物 | P0 |
| 查看宠物详情 | 获取单个宠物的详细信息 | P0 |
| 编辑宠物信息 | 修改宠物信息 | P1 |
| 删除宠物 | 删除宠物记录（软删除） | P1 |

### 高级功能

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 宠物状态管理 | 宠物状态（活跃、暂停、已删除） | P1 |
| 头像管理 | 宠物头像上传和管理 | P0 |
| 宠物年龄计算 | 根据生日计算宠物年龄 | P1 |
| 宠物标签 | 为宠物添加标签（如：温顺、活泼） | P2 |

### 数据展示

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 宠物统计 | 宠物数量、类型分布等统计信息 | P2 |
| 最近更新 | 最近添加的宠物 | P2 |
| 搜索功能 | 按名字搜索宠物 | P2 |

---

## 技术实现

### 目录结构

```
backend/src/modules/pet/
├── pet.module.ts            # 模块定义
├── pet.controller.ts        # 控制器
├── pet.service.ts           # 核心逻辑
└── dto/
    ├── create-pet.dto.ts    # 创建宠物 DTO
    └── update-pet.dto.ts    # 更新宠物 DTO
```

### 核心文件

#### 1. create-pet.dto.ts

```typescript
export class CreatePetDto {
  @IsString()
  @ApiProperty({ description: '宠物名字' })
  name: string;

  @IsString()
  @ApiProperty({ description: '宠物类型' })
  type: string;

  @IsString()
  @IsOptional()
  @ApiPropertyOptional({ description: '品种' })
  breed?: string;

  @IsInt()
  @Min(0)
  @Max(2)
  @IsOptional()
  @ApiPropertyOptional({ description: '性别 0:未知 1:公 2:母' })
  gender?: number;

  @IsDateString()
  @IsOptional()
  @ApiPropertyOptional({ description: '生日 YYYY-MM-DD' })
  birthday?: string;

  @IsString()
  @IsOptional()
  @ApiPropertyOptional({ description: '宠物简介' })
  bio?: string;

  @IsString()
  @IsOptional()
  @ApiPropertyOptional({ description: '头像 URL' })
  avatarUrl?: string;
}
```

#### 2. update-pet.dto.ts

```typescript
import { PartialType } from '@nestjs/swagger';
import { CreatePetDto } from './create-pet.dto';

export class UpdatePetDto extends PartialType(CreatePetDto) {}
```

#### 3. pet.service.ts 核心方法

```typescript
@Injectable()
export class PetService {
  constructor(private prisma: PrismaService) {}

  async findAllByUserId(userId: number) {
    return this.prisma.pet.findMany({
      where: { userId, isActive: true },
      orderBy: { createdAt: 'desc' },
    });
  }

  async findOne(id: number) {
    const pet = await this.prisma.pet.findUnique({
      where: { id },
      include: { user: { select: { id: true, username: true, avatarUrl: true } } },
    });

    if (!pet) {
      throw new NotFoundException('宠物不存在');
    }

    return pet;
  }

  async create(userId: number, createPetDto: CreatePetDto) {
    return this.prisma.pet.create({
      data: {
        ...createPetDto,
        userId,
        isActive: true,
      },
    });
  }

  async update(userId: number, petId: number, updatePetDto: UpdatePetDto) {
    const pet = await this.prisma.pet.findUnique({
      where: { id: petId },
    });

    if (!pet) {
      throw new NotFoundException('宠物不存在');
    }

    if (pet.userId !== userId) {
      throw new ForbiddenException('无权修改此宠物');
    }

    return this.prisma.pet.update({
      where: { id: petId },
      data: updatePetDto,
    });
  }

  async remove(userId: number, petId: number) {
    const pet = await this.prisma.pet.findUnique({
      where: { id: petId },
    });

    if (!pet) {
      throw new NotFoundException('宠物不存在');
    }

    if (pet.userId !== userId) {
      throw new ForbiddenException('无权删除此宠物');
    }

    return this.prisma.pet.update({
      where: { id: petId },
      data: { isActive: false },
    });
  }
}
```

---

## 开发进度

### 第一阶段：基础功能

**目标**: 实现宠物的基本 CRUD 操作

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 创建 DTO 文件 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 创建服务层 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 创建控制器 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 创建模块 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |

### 第二阶段：优化功能

**目标**: 完善功能和用户体验

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 添加头像上传 | 后端开发 | 2026-04-24 | 2026-04-25 | 🔄 进行中 |
| 完善错误处理 | 后端开发 | 2026-04-24 | 2026-04-25 | 🔄 进行中 |
| 添加 Swagger 文档 | 后端开发 | 2026-04-24 | 2026-04-25 | 🔄 进行中 |

### 第三阶段：测试

**目标**: 确保功能稳定和正确性

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 编写单元测试 | 测试人员 | 2026-04-24 | 2026-04-26 | 🔄 进行中 |
| 编写 E2E 测试 | 测试人员 | 2026-04-24 | 2026-04-26 | 🔄 进行中 |
| 手动测试 | 开发人员 | 2026-04-24 | 2026-04-26 | 🔄 进行中 |

---

## 接口列表

### 接口 1: 获取宠物列表

| 字段 | 内容 |
|------|------|
| **地址** | `GET /api/v1/pets` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "小白",
      "type": "狗",
      "breed": "金毛",
      "gender": 1,
      "birthday": "2020-01-01",
      "bio": "温柔的大金毛",
      "avatarUrl": null,
      "isActive": true,
      "createdAt": "2026-04-24T10:30:00.000Z"
    }
  ],
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 2: 创建宠物

| 字段 | 内容 |
|------|------|
| **地址** | `POST /api/v1/pets` |
| **认证** | Bearer Token |
| **请求格式** | `application/json` |

**请求参数**:
```json
{
  "name": "小黑",
  "type": "猫",
  "breed": "英国短毛猫",
  "gender": 1,
  "birthday": "2021-06-15",
  "bio": "高冷的小猫咪"
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "name": "小黑",
    "type": "猫",
    "breed": "英国短毛猫",
    "gender": 1,
    "birthday": "2021-06-15",
    "bio": "高冷的小猫咪",
    "avatarUrl": null,
    "isActive": true,
    "createdAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 3: 获取宠物详情

| 字段 | 内容 |
|------|------|
| **地址** | `GET /api/v1/pets/:id` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "小白",
    "type": "狗",
    "breed": "金毛",
    "gender": 1,
    "birthday": "2020-01-01",
    "bio": "温柔的大金毛",
    "avatarUrl": null,
    "isActive": true,
    "createdAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 4: 更新宠物

| 字段 | 内容 |
|------|------|
| **地址** | `PUT /api/v1/pets/:id` |
| **认证** | Bearer Token |
| **请求格式** | `application/json` |

**请求参数**:
```json
{
  "bio": "温柔又活泼的大金毛"
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "小白",
    "type": "狗",
    "breed": "金毛",
    "gender": 1,
    "birthday": "2020-01-01",
    "bio": "温柔又活泼的大金毛",
    "avatarUrl": null,
    "isActive": true,
    "updatedAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 5: 删除宠物

| 字段 | 内容 |
|------|------|
| **地址** | `DELETE /api/v1/pets/:id` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "小白",
    "isActive": false
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

---

## 测试计划

### 单元测试

**覆盖范围**:
- `pet.service.ts` 的主要方法
- 各个 DTO 的验证规则

**测试文件**:
```typescript
// test/unit/pet.service.spec.ts
describe('PetService', () => {
  describe('findAllByUserId', () => {
    it('should return user pets', async () => {
      const mockPets = [
        { id: 1, name: '小白', type: '狗' }
      ];
      (prisma.pet.findMany as jest.Mock).mockResolvedValue(mockPets);
      
      const result = await service.findAllByUserId(1);
      expect(result).toEqual(mockPets);
    });
  });
});
```

### E2E 测试

**测试场景**:
- 登录 -> 添加宠物 -> 查看宠物列表 -> 更新宠物 -> 删除宠物

**测试文件**:
```typescript
// test/e2e/pet.e2e-spec.ts
describe('Pet (e2e)', () => {
  describe('POST /pets', () => {
    it('should create a new pet', () => {
      return request(app.getHttpServer())
        .post('/pets')
        .set('Authorization', `Bearer ${token}`)
        .send({
          name: '小黑',
          type: '猫',
          breed: '英国短毛猫'
        })
        .expect(201);
    });
  });
});
```

---

## 验收标准

### 功能验收

1. **宠物管理**
   - ✅ 可以创建、查看、更新和删除宠物
   - ✅ 宠物列表显示正确的信息
   - ✅ 宠物信息可以正确保存和读取

2. **接口规范**
   - ✅ 统一的响应格式
   - ✅ 正确的状态码
   - ✅ 详细的错误信息
   - ✅ Swagger 文档

3. **权限验证**
   - ✅ 未认证用户无法访问需要认证的接口
   - ✅ 正确使用 Bearer Token

---

## 风险评估

### 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 并发操作 | 数据不一致 | 低 | 事务管理 |
| 大量数据 | 性能问题 | 低 | 分页和索引优化 |

### 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 宠物信息泄露 | 用户宠物数据暴露 | 低 | HTTPS 和加密传输 |
| 大量用户同时操作 | 服务压力 | 低 | 监控和限流 |

---

## 后续优化

### 功能扩展

- [ ] 宠物关系图（宠物之间的关系）
- [ ] 健康记录管理（疫苗、体检记录）
- [ ] 宠物行为记录（喂食、运动时间）

### 性能优化

- [ ] 宠物数据缓存
- [ ] 图片压缩和 CDN
- [ ] 分库分表（数据量大时）

---

**文档版本**: 1.0  
**最后更新**: 2026-04-24  
**负责人**: 后端开发团队

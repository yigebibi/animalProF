# 评论模块开发计划

---

## 📋 目录

1. [概述](#概述)
2. [功能需求](#功能需求)
3. [技术实现](#技术实现)
4. [开发进度](#开发进度)
5. [接口列表](#接口列表)
6. [测试计划](#测试计划)
7. [验收标准](#验收标准)
8. [风险评估](#风险评估)

---

## 概述

### 模块名称
评论模块 (Comment Module)

### 开发时间
**计划开始**: 2026-04-25  
**计划完成**: 2026-04-28  
**负责人**: 后端开发团队

### 模块说明
提供帖子评论功能，包括添加评论、查看评论、编辑评论和删除评论等。

### 依赖关系
- **认证模块**: 需要用户身份验证
- **帖子模块**: 评论与帖子关联
- **数据库**: 评论数据存储
- **通知模块**: 评论通知功能

---

## 功能需求

### 基本操作

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 添加评论 | 在帖子下发表评论 | P0 |
| 查看评论列表 | 获取帖子的所有评论 | P0 |
| 查看评论详情 | 获取单个评论的详细信息 | P1 |
| 编辑评论 | 修改评论内容 | P1 |
| 删除评论 | 删除评论记录 | P1 |

### 高级功能

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 回复评论 | 支持回复其他用户的评论 | P0 |
| 评论点赞 | 评论的点赞功能 | P2 |
| 评论收藏 | 收藏评论 | P2 |
| 评论标签 | 为评论添加标签 | P3 |
| 评论状态 | 评论状态（正常、删除、隐藏） | P1 |

### 交互功能

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 评论通知 | 评论时通知相关用户 | P2 |
| 评论标记已读 | 标记评论通知已读 | P3 |
| 评论搜索 | 按内容搜索评论 | P3 |
| 评论排序 | 按时间、点赞数排序 | P2 |

---

## 技术实现

### 目录结构

```
backend/src/modules/comment/
├── comment.module.ts        # 模块定义
├── comment.controller.ts    # 控制器
├── comment.service.ts       # 核心逻辑
└── dto/
    ├── create-comment.dto.ts   # 创建评论 DTO
    └── update-comment.dto.ts   # 更新评论 DTO
```

### 核心文件

#### 1. create-comment.dto.ts

```typescript
export class CreateCommentDto {
  @IsString()
  @ApiProperty({ description: '评论内容' })
  content: string;

  @IsInt()
  @IsOptional()
  @ApiPropertyOptional({ description: '父评论ID（回复评论时需要）' })
  parentId?: number;

  @IsInt()
  @ApiProperty({ description: '帖子ID' })
  postId: number;
}
```

#### 2. update-comment.dto.ts

```typescript
import { PartialType } from '@nestjs/swagger';
import { CreateCommentDto } from './create-comment.dto';

export class UpdateCommentDto extends PartialType(CreateCommentDto) {}
```

#### 3. comment.service.ts 核心方法

```typescript
@Injectable()
export class CommentService {
  constructor(private prisma: PrismaService) {}

  async findAllByPostId(postId: number) {
    return this.prisma.comment.findMany({
      where: { postId, isActive: true },
      orderBy: { createdAt: 'desc' },
      include: {
        user: { select: { id: true, username: true, avatarUrl: true } },
        children: { include: { user: { select: { id: true, username: true, avatarUrl: true } } } },
      },
    });
  }

  async findOne(id: number) {
    const comment = await this.prisma.comment.findUnique({
      where: { id },
      include: { user: { select: { id: true, username: true, avatarUrl: true } } },
    });

    if (!comment) {
      throw new NotFoundException('评论不存在');
    }

    return comment;
  }

  async create(userId: number, createCommentDto: CreateCommentDto) {
    const { postId, content, parentId } = createCommentDto;

    // 验证帖子是否存在
    const post = await this.prisma.post.findUnique({ where: { id: postId } });
    if (!post) {
      throw new NotFoundException('帖子不存在');
    }

    return this.prisma.comment.create({
      data: {
        content,
        postId,
        userId,
        parentId,
        isActive: true,
        likeCount: 0,
      },
    });
  }

  async update(userId: number, commentId: number, updateCommentDto: UpdateCommentDto) {
    const comment = await this.prisma.comment.findUnique({
      where: { id: commentId },
    });

    if (!comment) {
      throw new NotFoundException('评论不存在');
    }

    if (comment.userId !== userId) {
      throw new ForbiddenException('无权修改此评论');
    }

    return this.prisma.comment.update({
      where: { id: commentId },
      data: updateCommentDto,
    });
  }

  async remove(userId: number, commentId: number) {
    const comment = await this.prisma.comment.findUnique({
      where: { id: commentId },
    });

    if (!comment) {
      throw new NotFoundException('评论不存在');
    }

    if (comment.userId !== userId) {
      throw new ForbiddenException('无权删除此评论');
    }

    return this.prisma.comment.update({
      where: { id: commentId },
      data: { isActive: false },
    });
  }
}
```

---

## 开发进度

### 第一阶段：基础功能

**目标**: 实现基本的评论 CRUD 操作

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 创建 DTO 文件 | 后端开发 | 2026-04-25 | 2026-04-25 | ✅ 完成 |
| 创建服务层 | 后端开发 | 2026-04-25 | 2026-04-25 | ✅ 完成 |
| 创建控制器 | 后端开发 | 2026-04-25 | 2026-04-25 | ✅ 完成 |
| 创建模块 | 后端开发 | 2026-04-25 | 2026-04-25 | ✅ 完成 |

### 第二阶段：优化功能

**目标**: 完善功能和用户体验

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 添加评论通知 | 后端开发 | 2026-04-25 | 2026-04-26 | 🔄 进行中 |
| 完善错误处理 | 后端开发 | 2026-04-25 | 2026-04-26 | 🔄 进行中 |
| 添加 Swagger 文档 | 后端开发 | 2026-04-25 | 2026-04-26 | 🔄 进行中 |

### 第三阶段：测试

**目标**: 确保功能稳定和正确性

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 编写单元测试 | 测试人员 | 2026-04-26 | 2026-04-28 | 🔄 进行中 |
| 编写 E2E 测试 | 测试人员 | 2026-04-26 | 2026-04-28 | 🔄 进行中 |
| 手动测试 | 开发人员 | 2026-04-26 | 2026-04-28 | 🔄 进行中 |

---

## 接口列表

### 接口 1: 获取评论列表

| 字段 | 内容 |
|------|------|
| **地址** | `GET /api/v1/comments/post/:postId` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "content": "好可爱！",
      "postId": 1,
      "userId": 2,
      "isActive": true,
      "likeCount": 3,
      "createdAt": "2026-04-24T10:30:00.000Z",
      "user": {
        "id": 2,
        "username": "testuser2",
        "avatarUrl": null
      },
      "children": []
    }
  ],
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 2: 创建评论

| 字段 | 内容 |
|------|------|
| **地址** | `POST /api/v1/comments` |
| **认证** | Bearer Token |
| **请求格式** | `application/json` |

**请求参数**:
```json
{
  "content": "好可爱！",
  "postId": 1
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "content": "好可爱！",
    "postId": 1,
    "userId": 1,
    "isActive": true,
    "likeCount": 0,
    "createdAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 3: 获取评论详情

| 字段 | 内容 |
|------|------|
| **地址** | `GET /api/v1/comments/:id` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "content": "好可爱！",
    "postId": 1,
    "userId": 1,
    "isActive": true,
    "likeCount": 3,
    "createdAt": "2026-04-24T10:30:00.000Z",
    "user": {
      "id": 1,
      "username": "testuser",
      "avatarUrl": null
    }
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 4: 更新评论

| 字段 | 内容 |
|------|------|
| **地址** | `PUT /api/v1/comments/:id` |
| **认证** | Bearer Token |
| **请求格式** | `application/json` |

**请求参数**:
```json
{
  "content": "好可爱呀！"
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "content": "好可爱呀！",
    "updatedAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 5: 删除评论

| 字段 | 内容 |
|------|------|
| **地址** | `DELETE /api/v1/comments/:id` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "isActive": false
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

---

## 测试计划

### 单元测试

**覆盖范围**:
- `comment.service.ts` 的主要方法
- 各个 DTO 的验证规则

**测试文件**:
```typescript
// test/unit/comment.service.spec.ts
describe('CommentService', () => {
  describe('findAllByPostId', () => {
    it('should return post comments', async () => {
      const mockComments = [
        { id: 1, content: '好可爱！', postId: 1 }
      ];
      (prisma.comment.findMany as jest.Mock).mockResolvedValue(mockComments);
      
      const result = await service.findAllByPostId(1);
      expect(result).toEqual(mockComments);
    });
  });
});
```

### E2E 测试

**测试场景**:
- 登录 -> 创建评论 -> 查看评论列表 -> 更新评论 -> 删除评论

**测试文件**:
```typescript
// test/e2e/comment.e2e-spec.ts
describe('Comment (e2e)', () => {
  describe('POST /comments', () => {
    it('should create a new comment', () => {
      return request(app.getHttpServer())
        .post('/comments')
        .set('Authorization', `Bearer ${token}`)
        .send({
          content: '好可爱！',
          postId: 1
        })
        .expect(201);
    });
  });
});
```

---

## 验收标准

### 功能验收

1. **评论管理**
   - ✅ 可以创建、查看、更新和删除评论
   - ✅ 评论列表显示正确的信息
   - ✅ 评论信息可以正确保存和读取

2. **接口规范**
   - ✅ 统一的响应格式
   - ✅ 正确的状态码
   - ✅ 详细的错误信息
   - ✅ Swagger 文档

3. **权限验证**
   - ✅ 未认证用户无法访问需要认证的接口
   - ✅ 正确使用 Bearer Token

---

## 风险评估

### 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 评论回复层级 | 复杂的回复层级 | 低 | 使用扁平化数据结构 |
| 评论数量大 | 性能问题 | 低 | 分页加载和索引优化 |

### 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 垃圾评论 | 内容质量问题 | 低 | 评论审核机制 |
| 恶意评论 | 用户体验问题 | 低 | 举报和删除功能 |

---

## 后续优化

### 功能扩展

- [ ] 评论搜索功能
- [ ] 评论排序功能
- [ ] 评论点赞和收藏

### 性能优化

- [ ] 评论缓存
- [ ] 数据库查询优化
- [ ] 分库分表（数据量大时）

---

**文档版本**: 1.0  
**最后更新**: 2026-04-24  
**负责人**: 后端开发团队

# 认证模块开发计划

---

## 📋 目录

1. [概述](#概述)
2. [功能需求](#功能需求)
3. [技术实现](#技术实现)
4. [开发进度](#开发进度)
5. [接口列表](#接口列表)
6. [测试计划](#测试计划)
7. [验收标准](#验收标准)
8. [风险评估](#风险评估)

---

## 概述

### 模块名称
认证模块 (Auth Module)

### 开发时间
**计划开始**: 2026-04-24  
**计划完成**: 2026-04-25  
**负责人**: 后端开发团队

### 模块说明
提供用户身份认证功能，包括登录、注册、Token管理等，是整个系统的基础模块。

### 依赖关系
- **数据库**: PostgreSQL (已配置)
- **缓存**: Redis (已配置)
- **框架**: NestJS (已安装)

---

## 功能需求

### 用户登录

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 邮箱密码登录 | 用户通过邮箱和密码登录系统 | P0 |
| Token返回 | 登录成功返回 JWT Token | P0 |
| 用户信息返回 | 登录成功返回用户基本信息 | P1 |
| 登录失败处理 | 处理密码错误、用户不存在等情况 | P0 |
| 验证码 | 可选：防止暴力破解的图形验证码 | P2 |

### 用户注册

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 基本信息注册 | 用户名、邮箱、密码注册 | P0 |
| 邮箱验证 | 可选：注册后发送验证邮件 | P1 |
| 密码强度检测 | 密码强度评估 | P1 |
| 用户重复检查 | 检查用户名和邮箱是否已存在 | P0 |

### 其他功能

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| Token刷新 | 可选：提供 Token 刷新机制 | P2 |
| 登录日志 | 记录用户登录日志 | P2 |
| 账号锁定 | 连续失败登录锁定 | P1 |

---

## 技术实现

### 目录结构

```
backend/src/modules/auth/
├── auth.module.ts         # 模块定义
├── auth.controller.ts     # 控制器
├── auth.service.ts        # 核心逻辑
├── dto/
│   ├── login.dto.ts       # 登录请求参数
│   └── register.dto.ts    # 注册请求参数
└── strategies/
    └── jwt.strategy.ts    # JWT 策略
```

### 核心文件

#### 1. login.dto.ts

```typescript
export class LoginDto {
  @IsEmail()
  @ApiProperty({ description: '邮箱地址' })
  email: string;

  @IsString()
  @MinLength(6)
  @ApiProperty({ description: '密码，至少6位' })
  password: string;
}
```

#### 2. register.dto.ts

```typescript
export class RegisterDto {
  @IsString()
  @MinLength(3)
  @MaxLength(20)
  @ApiProperty({ description: '用户名，3-20字符' })
  username: string;

  @IsEmail()
  @ApiProperty({ description: '邮箱地址' })
  email: string;

  @IsString()
  @MinLength(6)
  @ApiProperty({ description: '密码，至少6位' })
  password: string;
}
```

#### 3. auth.service.ts 核心方法

```typescript
async login(loginDto: LoginDto) {
  // 1. 查找用户
  // 2. 验证密码
  // 3. 检查用户状态
  // 4. 生成 Token
  // 5. 返回结果
}

async register(registerDto: RegisterDto) {
  // 1. 检查用户是否已存在
  // 2. 密码加密
  // 3. 创建用户
  // 4. 生成 Token
  // 5. 返回结果
}
```

#### 4. jwt.strategy.ts

```typescript
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(
    private configService: ConfigService,
    private prisma: PrismaService,
  ) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: configService.get<string>('JWT_SECRET'),
    });
  }

  async validate(payload: any) {
    const user = await this.prisma.user.findUnique({
      where: { id: payload.userId },
    });
    if (!user) {
      throw new UnauthorizedException();
    }
    return { userId: user.id, role: user.role };
  }
}
```

---

## 开发进度

### 第一阶段：基础功能

**目标**: 实现基本的登录和注册功能

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 创建项目结构 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 创建 DTO 文件 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 创建控制器 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 创建服务层 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 配置 JWT 策略 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 配置认证模块 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |

### 第二阶段：优化功能

**目标**: 完善功能和用户体验

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 完善错误处理 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 优化响应格式 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 添加 Swagger 文档 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |

### 第三阶段：测试

**目标**: 确保功能稳定和正确性

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 编写单元测试 | 测试人员 | 2026-04-24 | 2026-04-25 | 🔄 进行中 |
| 编写 E2E 测试 | 测试人员 | 2026-04-24 | 2026-04-25 | 🔄 进行中 |
| 手动测试 | 开发人员 | 2026-04-24 | 2026-04-25 | 🔄 进行中 |

---

## 接口列表

### 接口 1: 用户登录

| 字段 | 内容 |
|------|------|
| **地址** | `POST /api/v1/auth/login` |
| **认证** | 无需认证 |
| **请求格式** | `application/json` |

**请求参数**:
```json
{
  "email": "test@example.com",
  "password": "password123"
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "testuser",
      "email": "test@example.com",
      "avatarUrl": null,
      "nickname": "测试用户",
      "role": 0
    }
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

**错误响应**:
```json
{
  "code": 401,
  "message": "密码错误",
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 2: 用户注册

| 字段 | 内容 |
|------|------|
| **地址** | `POST /api/v1/auth/register` |
| **认证** | 无需认证 |
| **请求格式** | `application/json` |

**请求参数**:
```json
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "password123"
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "testuser",
      "email": "test@example.com",
      "nickname": "testuser",
      "role": 0
    }
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

**错误响应**:
```json
{
  "code": 409,
  "message": "用户名已存在",
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

---

## 测试计划

### 单元测试

**覆盖范围**:
- `auth.service.ts` 的主要方法
- `jwt.strategy.ts` 的验证逻辑
- `dto` 的验证规则

**测试文件**:
```typescript
// test/unit/auth.service.spec.ts
describe('AuthService', () => {
  describe('login', () => {
    it('should login with valid credentials', async () => {
      // 测试逻辑
    });
    
    it('should fail with invalid credentials', async () => {
      // 测试逻辑
    });
  });
});
```

### E2E 测试

**测试场景**:
- 用户注册 -> 登录 -> 获取用户信息
- 错误密码登录
- 用户名已存在的注册

**测试文件**:
```typescript
// test/e2e/auth.e2e-spec.ts
describe('Auth (e2e)', () => {
  describe('POST /auth/login', () => {
    it('should login with valid credentials', () => {
      return request(app.getHttpServer())
        .post('/auth/login')
        .send({
          email: 'test@example.com',
          password: 'password123'
        })
        .expect(201);
    });
  });
});
```

---

## 验收标准

### 功能验收

1. **登录功能**
   - ✅ 密码正确时返回 Token
   - ✅ 密码错误时返回 401
   - ✅ 用户不存在时返回 404
   - ✅ Token 包含用户ID和角色信息

2. **注册功能**
   - ✅ 成功注册返回 Token 和用户信息
   - ✅ 用户名已存在时返回 409
   - ✅ 邮箱格式不正确时返回 400
   - ✅ 密码长度不足时返回 400

3. **接口规范**
   - ✅ 统一的响应格式
   - ✅ 正确的状态码
   - ✅ 详细的错误信息
   - ✅ Swagger 文档

---

## 风险评估

### 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 密码加密算法弱 | 用户密码易被破解 | 中 | 使用 bcrypt 加密 |
| Token 泄露 | 账户被他人使用 | 中 | HTTPS 传输 |
| SQL 注入 | 数据泄露 | 低 | 使用 Prisma ORM |

### 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 大量用户同时注册 | 服务压力过大 | 低 | 监控和限流 |
| 恶意注册 | 数据库垃圾数据 | 中 | 邮箱验证 |

---

## 后续优化

### 安全方面

- [ ] 登录次数限制
- [ ] 密码重置功能
- [ ] 账户锁定机制
- [ ] 双重身份认证

### 性能方面

- [ ] 登录缓存
- [ ] Token 刷新机制
- [ ] 用户状态缓存

---

**文档版本**: 1.0  
**最后更新**: 2026-04-24  
**负责人**: 后端开发团队

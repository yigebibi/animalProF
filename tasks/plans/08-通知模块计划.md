# 通知模块开发计划

---

## 📋 目录

1. [概述](#概述)
2. [功能需求](#功能需求)
3. [技术实现](#技术实现)
4. [开发进度](#开发进度)
5. [接口列表](#接口列表)
6. [测试计划](#测试计划)
7. [验收标准](#验收标准)
8. [风险评估](#风险评估)

---

## 概述

### 模块名称
通知模块 (Notification Module)

### 开发时间
**计划开始**: 2026-04-26  
**计划完成**: 2026-05-01  
**负责人**: 后端开发团队

### 模块说明
提供通知和消息功能，支持用户之间的互动和系统通知。

### 依赖关系
- **认证模块**: 需要用户身份验证
- **数据库**: 通知数据存储
- **其他模块**: 通知来自各个功能模块

---

## 功能需求

### 基本功能

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 通知发送 | 向用户发送通知 | P0 |
| 通知接收 | 用户接收通知 | P0 |
| 通知查看 | 查看通知列表 | P0 |
| 通知详情 | 查看通知详情 | P1 |

### 通知管理

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 通知分类 | 按通知类型分类显示 | P1 |
| 通知标记 | 标记通知已读/未读 | P1 |
| 通知删除 | 删除通知 | P2 |
| 通知设置 | 通知设置和偏好 | P3 |

### 通知类型

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 评论通知 | 评论时通知用户 | P0 |
| 点赞通知 | 点赞时通知用户 | P0 |
| 回复通知 | 回复时通知用户 | P0 |
| 关注通知 | 关注时通知用户 | P1 |
| 系统通知 | 系统发送的通知 | P2 |

### 高级功能

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 通知提醒 | 声音和震动提醒 | P2 |
| 通知推送 | 设备推送通知 | P3 |
| 通知频率 | 通知频率限制 | P2 |
| 通知统计 | 通知数据统计 | P3 |

---

## 技术实现

### 目录结构

```
backend/src/modules/notification/
├── notification.module.ts   # 模块定义
├── notification.controller.ts # 控制器
├── notification.service.ts  # 核心逻辑
└── dto/
    ├── notification-query.dto.ts  # 通知查询 DTO
    └── notification-info.dto.ts  # 通知信息 DTO
```

### 核心文件

#### 1. notification-query.dto.ts

```typescript
export class NotificationQueryDto {
  @IsBoolean()
  @IsOptional()
  @ApiPropertyOptional({ description: '是否只获取未读通知' })
  unreadOnly?: boolean;

  @IsString()
  @IsOptional()
  @ApiPropertyOptional({ description: '通知类型' })
  type?: string;

  @IsNumber()
  @IsOptional()
  @ApiPropertyOptional({ description: '页码' })
  page?: number = 1;

  @IsNumber()
  @IsOptional()
  @ApiPropertyOptional({ description: '每页数量' })
  limit?: number = 10;
}
```

#### 2. notification-info.dto.ts

```typescript
export class NotificationInfoDto {
  @ApiProperty({ description: '通知ID' })
  id: number;

  @ApiProperty({ description: '通知类型' })
  type: string;

  @ApiProperty({ description: '通知内容' })
  content: string;

  @ApiPropertyOptional({ description: '通知链接' })
  link?: string;

  @ApiProperty({ description: '是否已读' })
  isRead: boolean;

  @ApiProperty({ description: '创建时间' })
  createdAt: Date;
}
```

#### 3. notification.service.ts 核心方法

```typescript
@Injectable()
export class NotificationService {
  constructor(private prisma: PrismaService) {}

  async createNotification(
    userId: number,
    type: string,
    content: string,
    link?: string,
  ) {
    return this.prisma.notification.create({
      data: {
        userId,
        type,
        content,
        link,
        isRead: false,
      },
    });
  }

  async findAllByUserId(userId: number, query: NotificationQueryDto) {
    const { unreadOnly = false, type, page = 1, limit = 10 } = query;

    const where: Prisma.NotificationWhereInput = { userId };

    if (unreadOnly) {
      where.isRead = false;
    }

    if (type) {
      where.type = type;
    }

    const [notifications, total] = await Promise.all([
      this.prisma.notification.findMany({
        where,
        orderBy: { createdAt: 'desc' },
        take: limit,
        skip: (page - 1) * limit,
      }),
      this.prisma.notification.count({ where }),
    ]);

    return {
      total,
      page,
      limit,
      notifications,
    };
  }

  async findOne(id: number, userId: number) {
    const notification = await this.prisma.notification.findFirst({
      where: { id, userId },
    });

    if (!notification) {
      throw new NotFoundException('通知不存在');
    }

    return notification;
  }

  async markAsRead(id: number, userId: number) {
    const notification = await this.findOne(id, userId);

    return this.prisma.notification.update({
      where: { id },
      data: { isRead: true },
    });
  }

  async markAllAsRead(userId: number) {
    return this.prisma.notification.updateMany({
      where: { userId, isRead: false },
      data: { isRead: true },
    });
  }

  async delete(id: number, userId: number) {
    const notification = await this.findOne(id, userId);

    return this.prisma.notification.delete({ where: { id } });
  }

  async getUnreadCount(userId: number) {
    return this.prisma.notification.count({
      where: { userId, isRead: false },
    });
  }
}
```

---

## 开发进度

### 第一阶段：基础功能

**目标**: 实现基本的通知功能

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 创建 DTO 文件 | 后端开发 | 2026-04-26 | 2026-04-26 | ✅ 完成 |
| 创建服务层 | 后端开发 | 2026-04-26 | 2026-04-26 | ✅ 完成 |
| 创建控制器 | 后端开发 | 2026-04-26 | 2026-04-26 | ✅ 完成 |
| 创建模块 | 后端开发 | 2026-04-26 | 2026-04-26 | ✅ 完成 |

### 第二阶段：优化功能

**目标**: 完善功能和用户体验

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 添加通知分类 | 后端开发 | 2026-04-26 | 2026-04-27 | 🔄 进行中 |
| 完善错误处理 | 后端开发 | 2026-04-26 | 2026-04-27 | 🔄 进行中 |
| 添加 Swagger 文档 | 后端开发 | 2026-04-26 | 2026-04-27 | 🔄 进行中 |

### 第三阶段：测试

**目标**: 确保功能稳定和正确性

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 编写单元测试 | 测试人员 | 2026-04-27 | 2026-05-01 | 🔄 进行中 |
| 编写 E2E 测试 | 测试人员 | 2026-04-27 | 2026-05-01 | 🔄 进行中 |
| 手动测试 | 开发人员 | 2026-04-27 | 2026-05-01 | 🔄 进行中 |

---

## 接口列表

### 接口 1: 获取通知列表

| 字段 | 内容 |
|------|------|
| **地址** | `GET /api/v1/notifications` |
| **认证** | Bearer Token |
| **请求格式** | - |

**查询参数**:
```
unreadOnly: 是否只获取未读通知
type: 通知类型
page: 页码（默认 1）
limit: 每页数量（默认 10）
```

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 5,
    "page": 1,
    "limit": 10,
    "notifications": [
      {
        "id": 1,
        "type": "comment",
        "content": "用户 'testuser' 评论了你的帖子",
        "link": "/posts/1",
        "isRead": false,
        "createdAt": "2026-04-24T10:30:00.000Z"
      }
    ]
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 2: 获取通知详情

| 字段 | 内容 |
|------|------|
| **地址** | `GET /api/v1/notifications/:id` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "type": "comment",
    "content": "用户 'testuser' 评论了你的帖子",
    "link": "/posts/1",
    "isRead": false,
    "createdAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 3: 标记通知已读

| 字段 | 内容 |
|------|------|
| **地址** | `PUT /api/v1/notifications/:id/read` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "type": "comment",
    "content": "用户 'testuser' 评论了你的帖子",
    "link": "/posts/1",
    "isRead": true,
    "createdAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 4: 标记所有通知已读

| 字段 | 内容 |
|------|------|
| **地址** | `PUT /api/v1/notifications/read-all` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "count": 3
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 5: 删除通知

| 字段 | 内容 |
|------|------|
| **地址** | `DELETE /api/v1/notifications/:id` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "message": "通知已删除"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

---

## 测试计划

### 单元测试

**覆盖范围**:
- `notification.service.ts` 的主要方法
- 各个 DTO 的验证规则

**测试文件**:
```typescript
// test/unit/notification.service.spec.ts
describe('NotificationService', () => {
  describe('findAllByUserId', () => {
    it('should get user notifications', async () => {
      const mockNotifications = [
        { id: 1, type: 'comment', content: '用户评论了你的帖子' }
      ];
      (prisma.notification.findMany as jest.Mock).mockResolvedValue(mockNotifications);
      
      const result = await service.findAllByUserId(1, { page: 1, limit: 10 });
      expect(result.notifications.length).toBeGreaterThan(0);
    });
  });
});
```

### E2E 测试

**测试场景**:
- 登录 -> 获取通知 -> 标记通知已读 -> 标记所有通知已读

**测试文件**:
```typescript
// test/e2e/notification.e2e-spec.ts
describe('Notification (e2e)', () => {
  describe('GET /notifications', () => {
    it('should get notifications for authenticated user', () => {
      return request(app.getHttpServer())
        .get('/notifications')
        .set('Authorization', `Bearer ${token}`)
        .expect(200);
    });
  });
});
```

---

## 验收标准

### 功能验收

1. **通知管理**
   - ✅ 可以获取通知列表
   - ✅ 通知可以标记已读
   - ✅ 通知可以删除

2. **接口规范**
   - ✅ 统一的响应格式
   - ✅ 正确的状态码
   - ✅ 详细的错误信息
   - ✅ Swagger 文档

3. **权限验证**
   - ✅ 未认证用户无法访问需要认证的接口
   - ✅ 正确使用 Bearer Token

---

## 风险评估

### 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 通知并发 | 大量通知导致性能问题 | 低 | 异步发送和队列处理 |
| 通知推送 | 推送失败 | 中 | 重试机制和失败处理 |

### 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 通知频率 | 通知过多导致用户烦恼 | 中 | 通知频率限制和设置 |
| 通知内容 | 通知内容不当 | 低 | 内容审核机制 |

---

## 后续优化

### 功能扩展

- [ ] 通知提醒和推送
- [ ] 通知设置和偏好
- [ ] 通知统计和分析

### 性能优化

- [ ] 使用 WebSocket 推送通知
- [ ] 通知数据索引优化
- [ ] 通知发送队列

---

**文档版本**: 1.0  
**最后更新**: 2026-04-24  
**负责人**: 后端开发团队

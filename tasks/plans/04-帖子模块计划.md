# 帖子模块开发计划

---

## 📋 目录

1. [概述](#概述)
2. [功能需求](#功能需求)
3. [技术实现](#技术实现)
4. [开发进度](#开发进度)
5. [接口列表](#接口列表)
6. [测试计划](#测试计划)
7. [验收标准](#验收标准)
8. [风险评估](#风险评估)

---

## 概述

### 模块名称
帖子模块 (Post Module)

### 开发时间
**计划开始**: 2026-04-24  
**计划完成**: 2026-04-27  
**负责人**: 后端开发团队

### 模块说明
提供用户创建、查看、编辑和删除帖子的功能，支持多媒体内容（文字、图片、视频）。

### 依赖关系
- **认证模块**: 需要用户身份验证
- **文件模块**: 图片和视频上传
- **数据库**: 帖子数据存储
- **通知模块**: 帖子相关通知（如评论、点赞）

---

## 功能需求

### 基本操作

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 创建帖子 | 发布新帖子 | P0 |
| 查看帖子列表 | 获取用户的所有帖子 | P0 |
| 查看帖子详情 | 获取单个帖子的详细信息 | P0 |
| 编辑帖子 | 修改帖子内容 | P1 |
| 删除帖子 | 删除帖子记录 | P1 |

### 内容管理

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 文字内容 | 支持 Markdown 或富文本编辑 | P0 |
| 图片上传 | 支持上传图片 | P0 |
| 视频上传 | 支持上传视频 | P1 |
| 文件格式限制 | 图片格式（JPG、PNG），视频格式（MP4、AVI） | P0 |

### 高级功能

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 标签系统 | 为帖子添加标签 | P0 |
| 分类系统 | 帖子分类（如：日常、训练、健康） | P1 |
| 状态管理 | 帖子状态（发布、草稿、审核） | P1 |
| 访问控制 | 公开/私有帖子 | P2 |

### 交互功能

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 点赞功能 | 点赞和取消点赞 | P1 |
| 评论功能 | 评论和回复评论 | P0 |
| 分享功能 | 分享到其他平台 | P2 |
| 收藏功能 | 收藏帖子 | P2 |

### 数据展示

| 功能项 | 需求描述 | 优先级 |
|--------|----------|--------|
| 搜索功能 | 按标题、内容、标签搜索 | P2 |
| 排序功能 | 按时间、热度、评论数排序 | P1 |
| 分页功能 | 帖子列表分页 | P1 |

---

## 技术实现

### 目录结构

```
backend/src/modules/post/
├── post.module.ts           # 模块定义
├── post.controller.ts       # 控制器
├── post.service.ts          # 核心逻辑
└── dto/
    ├── create-post.dto.ts   # 创建帖子 DTO
    └── update-post.dto.ts   # 更新帖子 DTO
```

### 核心文件

#### 1. create-post.dto.ts

```typescript
export class CreatePostDto {
  @IsString()
  @ApiProperty({ description: '帖子标题' })
  title: string;

  @IsString()
  @ApiProperty({ description: '帖子内容' })
  content: string;

  @IsArray()
  @IsString({ each: true })
  @IsOptional()
  @ApiPropertyOptional({ description: '标签列表' })
  tags?: string[];

  @IsString()
  @IsOptional()
  @ApiPropertyOptional({ description: '分类' })
  category?: string;

  @IsArray()
  @IsString({ each: true })
  @IsOptional()
  @ApiPropertyOptional({ description: '图片 URL 列表' })
  images?: string[];

  @IsString()
  @IsOptional()
  @ApiPropertyOptional({ description: '视频 URL' })
  video?: string;
}
```

#### 2. update-post.dto.ts

```typescript
import { PartialType } from '@nestjs/swagger';
import { CreatePostDto } from './create-post.dto';

export class UpdatePostDto extends PartialType(CreatePostDto) {}
```

#### 3. post.service.ts 核心方法

```typescript
@Injectable()
export class PostService {
  constructor(private prisma: PrismaService) {}

  async findAllByUserId(userId: number) {
    return this.prisma.post.findMany({
      where: { userId, isActive: true },
      orderBy: { createdAt: 'desc' },
    });
  }

  async findOne(id: number) {
    const post = await this.prisma.post.findUnique({
      where: { id },
      include: {
        user: { select: { id: true, username: true, avatarUrl: true } },
        comments: {
          include: {
            user: { select: { id: true, username: true, avatarUrl: true } },
          },
        },
      },
    });

    if (!post) {
      throw new NotFoundException('帖子不存在');
    }

    return post;
  }

  async create(userId: number, createPostDto: CreatePostDto) {
    return this.prisma.post.create({
      data: {
        ...createPostDto,
        userId,
        isActive: true,
        likeCount: 0,
        commentCount: 0,
      },
    });
  }

  async update(userId: number, postId: number, updatePostDto: UpdatePostDto) {
    const post = await this.prisma.post.findUnique({
      where: { id: postId },
    });

    if (!post) {
      throw new NotFoundException('帖子不存在');
    }

    if (post.userId !== userId) {
      throw new ForbiddenException('无权修改此帖子');
    }

    return this.prisma.post.update({
      where: { id: postId },
      data: updatePostDto,
    });
  }

  async remove(userId: number, postId: number) {
    const post = await this.prisma.post.findUnique({
      where: { id: postId },
    });

    if (!post) {
      throw new NotFoundException('帖子不存在');
    }

    if (post.userId !== userId) {
      throw new ForbiddenException('无权删除此帖子');
    }

    return this.prisma.post.update({
      where: { id: postId },
      data: { isActive: false },
    });
  }
}
```

---

## 开发进度

### 第一阶段：基础功能

**目标**: 实现基本的 CRUD 操作

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 创建 DTO 文件 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 创建服务层 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 创建控制器 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |
| 创建模块 | 后端开发 | 2026-04-24 | 2026-04-24 | ✅ 完成 |

### 第二阶段：优化功能

**目标**: 完善功能和用户体验

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 添加图片和视频支持 | 后端开发 | 2026-04-25 | 2026-04-25 | 🔄 进行中 |
| 完善错误处理 | 后端开发 | 2026-04-25 | 2026-04-25 | 🔄 进行中 |
| 添加 Swagger 文档 | 后端开发 | 2026-04-25 | 2026-04-25 | 🔄 进行中 |

### 第三阶段：测试

**目标**: 确保功能稳定和正确性

| 任务 | 负责人 | 开始时间 | 完成时间 | 状态 |
|------|--------|----------|----------|------|
| 编写单元测试 | 测试人员 | 2026-04-25 | 2026-04-27 | 🔄 进行中 |
| 编写 E2E 测试 | 测试人员 | 2026-04-25 | 2026-04-27 | 🔄 进行中 |
| 手动测试 | 开发人员 | 2026-04-25 | 2026-04-27 | 🔄 进行中 |

---

## 接口列表

### 接口 1: 获取帖子列表

| 字段 | 内容 |
|------|------|
| **地址** | `GET /api/v1/posts` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "title": "今天带小白去公园",
      "content": "今天天气真好...",
      "tags": ["公园", "散步"],
      "category": "日常",
      "isActive": true,
      "createdAt": "2026-04-24T10:30:00.000Z"
    }
  ],
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 2: 创建帖子

| 字段 | 内容 |
|------|------|
| **地址** | `POST /api/v1/posts` |
| **认证** | Bearer Token |
| **请求格式** | `application/json` |

**请求参数**:
```json
{
  "title": "今天带小白去公园",
  "content": "今天天气真好，带小白去了公园散步。它玩得很开心！",
  "tags": ["公园", "散步"],
  "category": "日常"
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "title": "今天带小白去公园",
    "content": "今天天气真好，带小白去了公园散步。它玩得很开心！",
    "tags": ["公园", "散步"],
    "category": "日常",
    "isActive": true,
    "likeCount": 0,
    "commentCount": 0,
    "createdAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 3: 获取帖子详情

| 字段 | 内容 |
|------|------|
| **地址** | `GET /api/v1/posts/:id` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "title": "今天带小白去公园",
    "content": "今天天气真好...",
    "tags": ["公园", "散步"],
    "category": "日常",
    "isActive": true,
    "likeCount": 5,
    "commentCount": 3,
    "user": {
      "id": 1,
      "username": "testuser",
      "avatarUrl": null
    },
    "comments": [
      {
        "id": 1,
        "content": "看起来好开心！",
        "userId": 2,
        "createdAt": "2026-04-24T10:30:00.000Z"
      }
    ],
    "createdAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 4: 更新帖子

| 字段 | 内容 |
|------|------|
| **地址** | `PUT /api/v1/posts/:id` |
| **认证** | Bearer Token |
| **请求格式** | `application/json` |

**请求参数**:
```json
{
  "title": "今天带小白去公园散步"
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "title": "今天带小白去公园散步",
    "content": "今天天气真好...",
    "updatedAt": "2026-04-24T10:30:00.000Z"
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

### 接口 5: 删除帖子

| 字段 | 内容 |
|------|------|
| **地址** | `DELETE /api/v1/posts/:id` |
| **认证** | Bearer Token |
| **请求格式** | - |

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "isActive": false
  },
  "timestamp": "2026-04-24T10:30:00.000Z"
}
```

---

## 测试计划

### 单元测试

**覆盖范围**:
- `post.service.ts` 的主要方法
- 各个 DTO 的验证规则

**测试文件**:
```typescript
// test/unit/post.service.spec.ts
describe('PostService', () => {
  describe('findAllByUserId', () => {
    it('should return user posts', async () => {
      const mockPosts = [
        { id: 1, title: '今天带小白去公园' }
      ];
      (prisma.post.findMany as jest.Mock).mockResolvedValue(mockPosts);
      
      const result = await service.findAllByUserId(1);
      expect(result).toEqual(mockPosts);
    });
  });
});
```

### E2E 测试

**测试场景**:
- 登录 -> 创建帖子 -> 查看帖子列表 -> 更新帖子 -> 删除帖子

**测试文件**:
```typescript
// test/e2e/post.e2e-spec.ts
describe('Post (e2e)', () => {
  describe('POST /posts', () => {
    it('should create a new post', () => {
      return request(app.getHttpServer())
        .post('/posts')
        .set('Authorization', `Bearer ${token}`)
        .send({
          title: '今天带小白去公园',
          content: '今天天气真好...',
          tags: ['公园', '散步']
        })
        .expect(201);
    });
  });
});
```

---

## 验收标准

### 功能验收

1. **帖子管理**
   - ✅ 可以创建、查看、更新和删除帖子
   - ✅ 帖子列表显示正确的信息
   - ✅ 帖子信息可以正确保存和读取

2. **接口规范**
   - ✅ 统一的响应格式
   - ✅ 正确的状态码
   - ✅ 详细的错误信息
   - ✅ Swagger 文档

3. **权限验证**
   - ✅ 未认证用户无法访问需要认证的接口
   - ✅ 正确使用 Bearer Token

---

## 风险评估

### 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 大文件上传 | 性能问题 | 低 | 分块上传和进度显示 |
| 图片处理 | 图片压缩和格式转换 | 中 | 使用专门的图片处理库 |

### 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 内容审查 | 不恰当的内容 | 低 | 内容审核机制 |
| 大量数据 | 数据库性能 | 低 | 索引优化和缓存 |

---

## 后续优化

### 功能扩展

- [ ] 搜索功能优化
- [ ] 评论系统优化
- [ ] 文件上传进度

### 性能优化

- [ ] 帖子内容缓存
- [ ] 图片 CDN 加速
- [ ] 数据库查询优化

---

**文档版本**: 1.0  
**最后更新**: 2026-04-24  
**负责人**: 后端开发团队
